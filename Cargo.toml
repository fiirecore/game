[package]
name = "pokemon-game"
version = "0.5.0-beta.2021-12-16"
authors = ["Rhys H <donotdoughnut99@gmail.com>"]
edition = "2021"
description = "Pokemon FireRed Clone"
repository = "https://github.com/DoNotDoughnut/pokemon-game"
keywords = ["games", "pokemon"]
categories = ["games"]
build = "build/build.rs"
readme = "README.md"
include = ["/build", "/assets"]
publish = false

[features]
default = ["audio"]
audio = [
    "firecore-audio-builder",
    "firecore-battle-gui/audio",
    "firecore-pokedex-engine-builder/audio"
]
# discord = ["discord-rich-presence"]

[profile.dev.package."*"]
opt-level = 3

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
opt-level = "z"

[dependencies]
# Battle GUI, also re-exports game engine
firecore-battle-gui = { git = "https://github.com/DoNotDoughnut/firecore-engine", rev = "e9e3073", package = "firecore-battle-gui", default-features = false }
# firecore-battle-gui = { path = "../firecore/engine/crates/battle-gui", default-features = false }

firecore-world = { path = "world" }

firecore-battle = { git = "https://github.com/fiirecore/battle", rev = "e8a2c94", features = [
    "ai",
    "host",
    "default_engine_scripting",
] }
# firecore-battle = { path = "../firecore/battle", features = [#
#     "ai",
#     "host",
#     "default_engine_scripting",
# ] }

firecore-storage = { path = "storage" }

### Other

serde = { version = "1.0", features = ["derive"] }
rand = { version = "0.8", features = ["small_rng"], default-features = false }
tinystr = { version = "0.4", features = ["serde"], default-features = false }
crossbeam-channel = "0.5"
hashbrown = "0.9"

# discord-rich-presence = { version = "0.1", optional = true }

# [target.'cfg(not(target_arch = "wasm32"))'.dependencies]
# pico-args = "0.4"

### Build dependencies

[build-dependencies]

firecore-pokedex-builder = { git = "https://github.com/fiirecore-game/pokedex-builder", rev = "5f5643e" }
firecore-pokedex-engine-builder = { git = "https://github.com/DoNotDoughnut/firecore-pokedex-engine-builder", rev = "396e79e", default-features = false, features = ["compile"] }
firecore-battle-builder = { git = "https://github.com/DoNotDoughnut/firecore-battle-builder", rev = "1c253dc" }
firecore-world-builder = { path = "world/builder" }
firecore-font-builder = { git = "https://github.com/DoNotDoughnut/firecore-font-builder", rev = "057373a" }
firecore-audio-builder = { git = "https://github.com/DoNotDoughnut/firecore-audio-builder", rev = "90af8c1", optional = true }
serde = "1"
firecore-storage = { path = "storage", default-features = false }

[target.'cfg(windows)'.build-dependencies]
embed-resource = "1"


[workspace]
members = [
    "world",
    "world/builder",
    "storage"
]
